#include "model.h"
#include <vector>

namespace hack_game {
	extern Model
			platformModel,
			breakableCubeModel,
			unbreakableCubeModel,
			playerModel,
			playerBulletModel;
	
	extern const std::vector<Model*> models;
}